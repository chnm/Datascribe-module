<div id="sync-dataset" class="sidebar">
    <?php echo $this->hyperlink('', '#', ['class' => 'sidebar-close o-icon-close', 'title' => $this->translate('Close')]); ?>
    <h3><?php echo $this->translate('Sync dataset'); ?></h3>
    <?php if (!$dataset->itemSet()): ?>
    <p class="error"><?php echo $this->translate('This dataset has no item set. Set an item set to enable syncing.'); ?></p>
    <?php endif; ?>
    <p><?php echo $this->translate('Synchronize this dataset with the configured item set. Once synchronized, this dataset will contain every item in the item set. You should synchronize periodically, any time items are added or removed from the item set.'); ?></p>
    <p class="error"><?php echo $this->translate('Warning: items that have been removed from the configured item set since the last sync will be deleted from this dataset.'); ?></p>
    <?php echo $this->form($formSync); ?>
</div>

<div id="validate-dataset" class="sidebar">
    <?php echo $this->hyperlink('', '#', ['class' => 'sidebar-close o-icon-close', 'title' => $this->translate('Close')]); ?>
    <h3><?php echo $this->translate('Validate dataset'); ?></h3>
    <p><?php echo $this->translate('Validate this dataset against the rules currently set in the form builder. Once validated, invalid values will be marked as invalid. You should validate periodically, after fields are added to the form, after field validation rules change, and before exporting the dataset.'); ?></p>
    <?php echo $this->form($formValidate); ?>
</div>

<div id="export-dataset" class="sidebar">
    <?php echo $this->hyperlink('', '#', ['class' => 'sidebar-close o-icon-close', 'title' => $this->translate('Close')]); ?>
    <h3><?php echo $this->translate('Export dataset'); ?></h3>
    <p><?php echo $this->translate('Export this dataset to a CSV file. Note that the file will only include records from approved items, and that invalid values will be null. This will overwrite an existing export file.'); ?></p>
    <?php echo $this->form($formExport); ?>
</div>

<div id="move-dataset" class="sidebar">
    <?php echo $this->hyperlink('', '#', ['class' => 'sidebar-close o-icon-close', 'title' => $this->translate('Close')]); ?>
    <h3><?php echo $this->translate('Move dataset'); ?></h3>
    <p><?php echo $this->translate('Move this dataset to another project.'); ?></p>
    <?php echo $this->form($formMove); ?>
</div>
